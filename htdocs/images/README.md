# Iconos

Coloca aquí `icon.png` (64×64 PNG, fondo transparente) antes de empaquetar la
extensión para el Plesk Catalog. El manifest `meta.xml` lo referencia como
`htdocs/images/icon.png`.

Mientras no exista el archivo Plesk muestra un icono genérico — no rompe la
extensión, solo afea el listado.
