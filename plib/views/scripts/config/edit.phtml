<?php /** @var Zend_View $this */ ?>
<link rel="stylesheet" href="<?= pm_Context::getBaseUrl() ?>css/extension.css">

<p class="ec-back">
    <a href="<?= pm_Context::getBaseUrl() ?>index.php/index/index">
        ← <?= $this->escape($this->lmsg('btnBack')) ?>
    </a>
</p>

<h2><?= $this->escape($this->lmsg('configTitle')) ?></h2>

<section class="ec-card">
    <h3><?= $this->escape($this->lmsg('passwordResetTitle')) ?></h3>
    <p class="ec-hint"><?= $this->escape($this->lmsg('passwordResetHelp')) ?></p>

    <form method="post" action="<?= pm_Context::getBaseUrl() ?>index.php/config/edit" class="ec-form" autocomplete="off">
        <input type="hidden" name="do" value="reset-admin-password">

        <label>
            <?= $this->escape($this->lmsg('lblNewPassword')) ?>
            <input type="password" name="newPassword" required minlength="8" autocomplete="new-password">
        </label>

        <label>
            <?= $this->escape($this->lmsg('lblConfirmPassword')) ?>
            <input type="password" name="confirmPassword" required minlength="8" autocomplete="new-password">
        </label>

        <p>
            <button type="submit" class="btn btn-primary">
                <?= $this->escape($this->lmsg('btnResetPassword')) ?>
            </button>
        </p>
    </form>
</section>
